# TODO — 2026-05-22 이어서 할 작업

## 현재 상태

- Git: `feature/rag-v9-spec-rag` 브랜치, commit `8279c03` pushed
- Lambda (`lambda-rtl-parser-seoul-dev`): 배포됨, 512MB/300초/concurrency 20
- Qdrant: running (10.20.1.217:6333, us-east-1)
- Nginx Proxy: running (10.10.1.62:443, Frontend VPC) — `llm/mcp.corp.bos-semi.com` → API GW
- 인덱싱: tt_20260516 **재인덱싱 필요** (이전 인덱싱 시 QDRANT_ENDPOINT=null로 전부 skip됨)

## 해야 할 것 (순서대로)

### 1. Terraform provider 수정

**문제**: `llm-gateway-litellm.tf`, `llm-gateway-mcp.tf`의 일부 리소스에 `provider = aws.seoul`이 누락. 기본 provider가 us-east-1이라 Seoul VPC를 못 찾음.

**수정 대상** (SG, CloudWatch Alarm에 `provider = aws.seoul` 추가):

```
environments/app-layer/bedrock-rag/llm-gateway-litellm.tf:
  - aws_security_group.litellm → provider = aws.seoul 추가
  - aws_security_group_rule.litellm_* → provider = aws.seoul
  - aws_cloudwatch_metric_alarm.litellm_cpu_high → provider = aws.seoul
  - aws_cloudwatch_metric_alarm.litellm_status_check_failed → provider = aws.seoul
  - aws_cloudwatch_log_group.litellm → provider = aws.seoul

environments/app-layer/bedrock-rag/llm-gateway-mcp.tf:
  - aws_security_group.mcp_server → provider = aws.seoul 추가
  - aws_security_group_rule.mcp_* → provider = aws.seoul
  - aws_cloudwatch_metric_alarm.mcp_* → provider = aws.seoul
  - aws_cloudwatch_log_group.mcp_server → provider = aws.seoul
```

**검증**:
```bash
cd environments/app-layer/bedrock-rag
terraform validate
terraform plan  # Error 없이 Plan 나와야 함
```

### 2. QDRANT_ENDPOINT를 Terraform Lambda 환경변수에 영구 등록

**문제**: `rtl-parser-lambda.tf`의 Lambda 환경변수에 `QDRANT_ENDPOINT`가 없어서 terraform apply 때마다 날아감.

**수정**: `environments/app-layer/bedrock-rag/rtl-parser-lambda.tf`에서 Lambda function의 `environment.variables`에 추가:

```hcl
environment {
  variables = {
    ...기존 변수들...
    QDRANT_ENDPOINT   = "http://10.20.1.217:6333"
    QDRANT_COLLECTION = "rtl-knowledge-base"
  }
}
```

### 3. Terraform Apply (전체)

```bash
cd environments/app-layer/bedrock-rag
terraform apply -auto-approve
```

이러면:
- LiteLLM EC2 생성 (Logging VPC)
- MCP Server EC2 생성 (Frontend VPC)
- API Gateway /llm, /mcp 경로 + integration 생성
- SNS Topic, Secrets Manager 생성
- CloudWatch Alarms 생성
- Lambda 환경변수에 QDRANT_ENDPOINT 영구 등록

### 4. tt_20260516 재인덱싱

Apply 완료 후 (QDRANT_ENDPOINT 영구 설정된 상태):

```bash
py scripts/reindex_all_rtl.py --pipeline-id tt_20260516 --batch-size 10 --batch-delay 5
```

### 5. 검증

```bash
# Qdrant 검색 확인
py -c "
import boto3, json
lc = boto3.client('lambda', region_name='ap-northeast-2')
payload = {'action': 'search', 'query': 'trinity', 'pipeline_id': 'tt_20260516', 'max_results': 3}
resp = lc.invoke(FunctionName='lambda-rtl-parser-seoul-dev', InvocationType='RequestResponse', Payload=json.dumps(payload).encode())
result = json.loads(resp['Payload'].read())
print(f'total_hits: {result.get(\"total_hits\", 0)}')
"

# Nginx → API GW → LiteLLM 확인
curl -k https://llm.corp.bos-semi.com/v1/models

# Nginx → API GW → MCP 확인
curl -k https://mcp.corp.bos-semi.com/health
```

---

## 참고 정보

### Lambda 비용 설정
- Memory: 512MB (이전 2048MB)
- Timeout: 300초 (이전 900초)
- Reserved Concurrency: 20

### Nginx Proxy
- AMI: `ami-0b986c557c66c1c20` (nginx pre-installed, self 소유)
- User Data: `systemctl restart nginx` (AMI에서 이미 enable됨)
- Frontend VPC에 NAT 없음 → dnf install 불가 → 사전 빌드 AMI 필수

### 사내 DNS
BIND zone 파일 (`/var/cache/bind/corp.bos-semi.com.zone`):
```
llm  IN  A  10.10.1.62
mcp  IN  A  10.10.1.62
```
NS delegation은 Route53 ALIAS가 외부 forwarder에서 empty name 반환하여 실패 → A 레코드 직접 등록.

### API Gateway
- ID: `r0qa9lzhgi`
- URL: `https://r0qa9lzhgi.execute-api.ap-northeast-2.amazonaws.com/prod/`
- `/llm/{proxy+}` → LiteLLM EC2:4000 (아직 미배포)
- `/mcp/{proxy+}` → MCP EC2:3000 (아직 미배포)

### Qdrant EC2
- Instance: `i-0d520340617eb5484` (us-east-1)
- IP: 10.20.1.217:6333
- Collection: `rtl-knowledge-base`
- Lambda SG egress 6333 → 10.20.0.0/16 열려있음
